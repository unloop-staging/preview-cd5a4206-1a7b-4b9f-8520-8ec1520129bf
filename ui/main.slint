// Generated by Unloop - 2025-12-31T17:37:15.528Z
// Office Thermostat: Smart temperature control for your home office workspace

import { MaterialWindow } from "@material";
import { AppData } from "data.slint";
import { HomeScreen } from "screens.slint";
import { ControlScreen } from "screens.slint";
import { SettingsScreen } from "screens.slint";

export component App inherits MaterialWindow {
    title: "Office Thermostat";
    width: 800px;
    height: 480px;
    background: #1E1E1E;
    
    in-out property <string> active-screen: "home";
    in-out property <AppData> data: {
        current-temp: 72,
        target-temp: 72,
        humidity: 45,
        hvac-status: "ok",
        schedule-status: "ok",
        temp-unit: "°F",
        screen-timeout: 120,
        work-hours-enabled: true,
        away-detection: false,
        firmware-version: "2.1.0",

    };
    
    callback boost-heat-clicked();
    callback away-mode-clicked();
    callback target-changed(float);
    callback mode-changed(string);
    
    VerticalLayout {
        Rectangle {
            vertical-stretch: 1;
            
            if active-screen == "home" : HomeScreen {
                data <=> root.data;
            }

            if active-screen == "control" : ControlScreen {
                data <=> root.data;
            }

            if active-screen == "settings" : SettingsScreen {
                data <=> root.data;
            }

        }
        
        Rectangle {
            height: 80px;
            background: #2A2A2A;
            
            HorizontalLayout {
                alignment: space-evenly;
                
                Rectangle {
                    horizontal-stretch: 1;
                    
                    TouchArea {
                        clicked => { active-screen = "home"; }
                    }
                    
                    VerticalLayout {
                        alignment: center;
                        spacing: 4px;
                        
                        Rectangle {
                            width: 24px;
                            height: 24px;
                        }
                        
                        Text {
                            text: "Home";
                            font-size: 12px;
                            color: active-screen == "home" ? #3AA1F5 : #9E9E9E;
                            horizontal-alignment: center;
                        }
                    }
                }
                Rectangle {
                    horizontal-stretch: 1;
                    
                    TouchArea {
                        clicked => { active-screen = "control"; }
                    }
                    
                    VerticalLayout {
                        alignment: center;
                        spacing: 4px;
                        
                        Rectangle {
                            width: 24px;
                            height: 24px;
                        }
                        
                        Text {
                            text: "Control";
                            font-size: 12px;
                            color: active-screen == "control" ? #3AA1F5 : #9E9E9E;
                            horizontal-alignment: center;
                        }
                    }
                }
                Rectangle {
                    horizontal-stretch: 1;
                    
                    TouchArea {
                        clicked => { active-screen = "settings"; }
                    }
                    
                    VerticalLayout {
                        alignment: center;
                        spacing: 4px;
                        
                        Rectangle {
                            width: 24px;
                            height: 24px;
                        }
                        
                        Text {
                            text: "Settings";
                            font-size: 12px;
                            color: active-screen == "settings" ? #3AA1F5 : #9E9E9E;
                            horizontal-alignment: center;
                        }
                    }
                }
            }
        }
    }
}
